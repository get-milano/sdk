android.useAndroidX=true
# AGP 9 packages the APK in-process and needs more than Gradle's default
# heap; without this, :app:packageDebug dies with an OutOfMemoryError that
# surfaces only as "IncrementalSplitterRunnable failed".
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
