name: Release Swift

# Manual release of the signed XCFramework. Builds all platform slices from
# the Tuist framework project, signs with the Apple Distribution identity
# from repo secrets, rewrites Package.swift to a binaryTarget, and tags that
# commit: the tag resolves to the prebuilt binary, main stays source.
# See RELEASING.md for the certificate and secret setup.

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to release (e.g. 0.1.0); the tag becomes v<version>"
        required: true

permissions:
  contents: write

jobs:
  xcframework:
    runs-on: macos-latest
    env:
      VERSION: ${{ inputs.version }}
    steps:
      - name: Validate version
        run: |
          if ! [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "version must be semantic (e.g. 0.1.0), got: $VERSION" >&2
            exit 1
          fi

      - uses: actions/checkout@v4

      - name: Import signing certificate
        env:
          APPLE_CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_KEYCHAIN_PASSWORD: ${{ secrets.APPLE_KEYCHAIN_PASSWORD }}
        run: |
          KEYCHAIN="$RUNNER_TEMP/release.keychain-db"
          echo "$APPLE_CERTIFICATE_BASE64" | base64 --decode > "$RUNNER_TEMP/certificate.p12"
          security create-keychain -p "$APPLE_KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p "$APPLE_KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security import "$RUNNER_TEMP/certificate.p12" -k "$KEYCHAIN" \
            -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign -T /usr/bin/security
          security set-key-partition-list -S apple-tool:,apple: \
            -k "$APPLE_KEYCHAIN_PASSWORD" "$KEYCHAIN" > /dev/null
          security list-keychains -d user -s "$KEYCHAIN" login.keychain-db

      - name: Generate the framework project
        working-directory: engine/swiftui
        run: |
          brew install tuist
          tuist generate --no-open

      - name: Archive all slices
        working-directory: engine/swiftui
        run: |
          for destination in \
            "generic/platform=iOS" \
            "generic/platform=iOS Simulator" \
            "generic/platform=macOS" \
            "generic/platform=watchOS" \
            "generic/platform=watchOS Simulator"; do
            name="${destination#generic/platform=}"
            name="${name// /-}"
            xcodebuild archive \
              -workspace MilanoSDK.xcworkspace \
              -scheme MilanoSDK \
              -destination "$destination" \
              -archivePath "build/$name.xcarchive" \
              SKIP_INSTALL=NO BUILD_LIBRARY_FOR_DISTRIBUTION=YES \
              CODE_SIGNING_ALLOWED=NO | tail -3
          done

      - name: Create and sign the XCFramework
        working-directory: engine/swiftui
        run: |
          args=()
          for archive in build/*.xcarchive; do
            args+=(-framework "$archive/Products/Library/Frameworks/MilanoSDK.framework")
          done
          xcodebuild -create-xcframework "${args[@]}" -output build/MilanoSDK.xcframework
          codesign --timestamp --sign "Apple Distribution" build/MilanoSDK.xcframework
          codesign --verify --verbose build/MilanoSDK.xcframework
          ditto -c -k --sequesterRsrc --keepParent \
            build/MilanoSDK.xcframework "$RUNNER_TEMP/MilanoSDK.xcframework.zip"

      - name: Rewrite Package.swift for the tag
        run: |
          CHECKSUM=$(swift package compute-checksum "$RUNNER_TEMP/MilanoSDK.xcframework.zip")
          cat > Package.swift <<EOF
          // swift-tools-version:6.0
          import PackageDescription

          // Release manifest: this tag resolves to the prebuilt, signed
          // XCFramework attached to the GitHub release. The source package
          // lives on main.
          let package = Package(
              name: "milano-sdk",
              platforms: [
                  .iOS(.v15),
                  .macOS(.v12),
                  .watchOS(.v8)
              ],
              products: [
                  .library(name: "MilanoSDK", targets: ["MilanoSDK"])
              ],
              targets: [
                  .binaryTarget(
                      name: "MilanoSDK",
                      url: "https://github.com/get-milano/sdk/releases/download/v$VERSION/MilanoSDK.xcframework.zip",
                      checksum: "$CHECKSUM"
                  )
              ]
          )
          EOF

      - name: Tag and publish the release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Package.swift
          git commit -m "Release v$VERSION: binary Package.swift"
          git tag "v$VERSION"
          git push origin "v$VERSION"
          gh release create "v$VERSION" "$RUNNER_TEMP/MilanoSDK.xcframework.zip" \
            --title "v$VERSION" \
            --notes "Signed MilanoSDK.xcframework (iOS, iOS Simulator, macOS, watchOS, watchOS Simulator). SwiftPM: pin the \`v$VERSION\` tag to resolve the prebuilt binary." \
            || gh release upload "v$VERSION" "$RUNNER_TEMP/MilanoSDK.xcframework.zip" --clobber

      - name: Clean up keychain
        if: always()
        run: security delete-keychain "$RUNNER_TEMP/release.keychain-db" || true
