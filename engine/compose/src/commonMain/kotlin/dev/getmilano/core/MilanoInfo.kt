package dev.getmilano

/**
 * Milano SDK: a client-only, design-system-agnostic Document-Driven UI framework for Compose.
 *
 * This module implements the Milano specifications (see the specs repository).
 * The conformance suite there is the definition of done for this engine.
 */
object MilanoInfo {
    /**
     * The engine implementation version. The Milano contract version this engine
     * supports is declared by the gate.
     */
    const val VERSION: String = "1.0.0"
}
